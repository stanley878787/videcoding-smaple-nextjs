# Database Configuration
# Copy this file to .env.local and fill in your actual credentials
#
# ⚠️ NOTE: You can skip this step for local UI development!
# The app will run without a database, but data persistence features won't work.

# ============================================
# Quick Start Options
# ============================================

# Option 1: Neon (Free PostgreSQL - Recommended for Beginners)
# Sign up at https://neon.tech and paste your connection string below:
# DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require"

# Option 2: Local Docker PostgreSQL
# Run: pnpm db:start
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/breakfast_delivery?schema=public"

# Option 3: SQL Server (Advanced)
# DATABASE_URL="sqlserver://localhost:1433;database=breakfast_delivery;user=sa;password=YourStrong@Password123;encrypt=true;trustServerCertificate=true"

# ============================================
# Database Provider Selection (for reference)
# ============================================
DATABASE_PROVIDER="postgresql"

# ============================================
# SQL Server Configuration
# ============================================
# For Docker local development:
SQLSERVER_URL="sqlserver://localhost:1433;database=breakfast_delivery;user=sa;password=YourStrong@Password123;encrypt=true;trustServerCertificate=true"

# For Azure SQL (production):
# SQLSERVER_URL="sqlserver://server.database.windows.net:1433;database=dbname;user=username@server;password=password;encrypt=true"

# ============================================
# Active Database URL
# ============================================
# Set this to either POSTGRES_URL or SQLSERVER_URL
# This is the connection string Prisma will use
DATABASE_URL="${POSTGRES_URL}" 

# Quick switch examples:
# For PostgreSQL: DATABASE_URL="${POSTGRES_URL}"
# For SQL Server:  DATABASE_URL="${SQLSERVER_URL}"

# ============================================
# Notes
# ============================================
# 1. To switch databases:
#    - Method 1: Run `npm run db:switch` (recommended)
#    - Method 2: Manually update DATABASE_URL and prisma/schema.prisma provider
#
# 2. For Docker development:
#    - Start databases: npm run db:start
#    - Both PostgreSQL and SQL Server will be available
#
# 3. Password requirements for SQL Server:
#    - At least 8 characters
#    - Must contain uppercase, lowercase, numbers, and special characters
#
# 4. After changing DATABASE_URL:
#    - Run: npm run prisma:generate
#    - Run: npm run prisma:push (or prisma:migrate)
